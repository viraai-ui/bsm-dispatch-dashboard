import { NextRequest,NextResponse } from 'next/server'
import { getPublicDatabaseSnapshot,normalize } from '@/lib/public-database-snapshot'
export const dynamic='force-dynamic'
export async function GET(req:NextRequest){const started=performance.now();const snap=await getPublicDatabaseSnapshot();const q=(req.nextUrl.searchParams.get('q')||'').slice(0,200);const filter=req.nextUrl.searchParams.get('filter')||'all';const page=Math.max(1,Math.min(10000,Number(req.nextUrl.searchParams.get('page'))||1));const limit=Math.max(1,Math.min(50,Number(req.nextUrl.searchParams.get('limit'))||25));const needle=normalize(q);const found=snap.rows.filter(r=>(!q||snap.haystacks[r.id].includes(q.toLowerCase())||snap.haystacks[r.id].includes(needle))&&(filter==='all'||filter==='builty'&&r.builtyUploaded||filter==='closed'&&/closed/i.test(r.mediaLabel+r.lifecycleLabel)||filter==='submitted'&&/submitted/i.test(r.mediaLabel)||filter==='pending'&&/pending/i.test(r.mediaLabel)));const res=NextResponse.json({items:found.slice((page-1)*limit,page*limit),page,limit,total:found.length,pages:Math.ceil(found.length/limit),snapshotVersion:snap.snapshotVersion,generatedAt:snap.generatedAt});res.headers.set('Cache-Control','public, max-age=15, s-maxage=60, stale-while-revalidate=300');res.headers.set('Server-Timing',`search;dur=${(performance.now()-started).toFixed(1)}`);res.headers.set('X-Snapshot-Version',snap.snapshotVersion);return res}
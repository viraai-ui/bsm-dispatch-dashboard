import { DashboardShell } from '@/components/DashboardShell'
import { ModuleHeader, QueueTable, SyncSettingsCard } from '@/components/ModulePages'
export default function SyncMonitorPage() { return <DashboardShell active="Sync Monitor"><ModuleHeader moduleKey="sync" /><section className="grid two"><SyncSettingsCard /><div className="card"><h2>Zoho sync log</h2><p className="muted">Latest sales-order webhook received, backup sync planned, and conflict queue visible here.</p><button className="btn light full">View conflict review</button></div></section><div style={{ height: 16 }} /><QueueTable kind="sync" /></DashboardShell> }
